const glow=document.querySelector('.glow');window.addEventListener('pointermove',e=>{if(glow){glow.style.left=e.clientX+'px';glow.style.top=e.clientY+'px';}});const io=new IntersectionObserver(es=>{es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')})},{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>io.observe(el));let current=null;document.querySelectorAll('.play').forEach(btn=>{btn.addEventListener('click',()=>{const card=btn.closest('.card');const a=card.querySelector('audio');if(current&&current!==a){current.pause();current.currentTime=0;document.querySelectorAll('.play').forEach(b=>b.textContent='▶')}if(!a.src)a.src=btn.dataset.audio;if(!a.paused){a.pause();btn.textContent='▶';return}a.play().then(()=>{current=a;btn.textContent='Ⅱ'}).catch(()=>alert('sample-original.mp3 / sample-midi.mp3 파일을 assets 폴더에 넣으면 재생됩니다.'));a.onended=()=>btn.textContent='▶';})});
