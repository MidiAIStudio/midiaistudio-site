# Optional local/emulator fallback (gitignored real file: functions/.env)
# Production should use Firebase Secret Manager:
#   firebase functions:secrets:set DISCORD_INQUIRY_WEBHOOK
#   firebase functions:secrets:set DISCORD_PAYMENT_WEBHOOK
# Then deploy:
#   firebase deploy --only functions:notifyDiscordOnInquiryCreate,functions:notifyDiscordOnOrderCompleted

DISCORD_INQUIRY_WEBHOOK=
DISCORD_PAYMENT_WEBHOOK=
