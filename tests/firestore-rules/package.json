{
  "name": "midiai-firestore-rules-tests",
  "private": true,
  "scripts": {
    "test": "mocha firestore.rules.test.js --timeout 60000",
    "test:emulators": "firebase emulators:exec --project demo-midiai --only firestore \"npm test\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.0.0",
    "firebase-tools": "^13.0.0",
    "mocha": "^10.0.0"
  }
}
